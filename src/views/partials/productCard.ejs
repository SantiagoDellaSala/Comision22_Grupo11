
<article class="productCard_article">
    <h3><%= product.name %></h3>
    <a href="/products/detail/<%= product.id %>">
        <img
        src="/images/productos/<%=product.mainImage || 'ANAFE-PORTATIL-BUTANO.jpg'%>"
        alt="imagen de producto"
        />
    </a>
    
    <div class="productCard_div">
        <p>$<%= toThousand(product.price) %></p>
        <% if (product.discount) { %>
        <span>%<%= product.discount %> OFF</span>
        <% } %>
  </div>
  <button onclick="addToCart('<%=product.id%>','<%=product.name%>','<%=product.price%>','<%=product.mainImage%>')">Agregar al carrito</button>
</article>